# Local development authentication rules for Saayro.
# This keeps host access predictable for local tools like TablePlus
# while staying scoped to local development only.

local   all             all                                     trust
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust
host    all             all             all                     trust
